

<ul class="nav nav-list participants" id="participant_list">
  <li class="nav-header">This Episode's Geeks</li>
  <%= render :partial => 'participant',
            :collection => participants,
            :locals => {:current_user=>current_user} %>
  <%= render :partial => 'participant_form',
            :locals => {:episode => episode, :current_user => current_user} if current_user.present?%>
</ul>
