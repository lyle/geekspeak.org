<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? h( yield :title) : "GeekSpeak, KUSP" %> <%= content_for?(:subtitle) ? h( "- #{yield :subtitle}")  : ""  %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <link href="/favicon.ico" rel="shortcut icon">
    <link href="/images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="/images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="/images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">GeekSpeak</a>

          <div class="container nav-collapse">
            <ul class="nav">
                <li><%= link_to "Episodes", "/episodes"  %></li>
				<li class="dropdown"><a id="blogDrop" href="#" role="button" class="dropdown-toggle" data-toggle="dropdown">Blog <b class="caret"></b></a>
					<ul class="dropdown-menu" role="menu" aria-labelledb="blogDrop">
						<li>
							<a href="http://blog.geekspeak.org/">GeekSpeak Blog</a>
						</li>
						<li>
							<a href="http://tumblr.com/follow/geekspeak-kusp">Follow</a>
						</li>
						<li>
							<a href="http://blog.geekspeak.org/archive">Archive</a>
						</li>
						<li>
							<a href="http://blog.geekspeak.org/ask">Ask questions...</a>
						</li>
					</ul>
				</li>
                <li><%= link_to "Contact Us", "/contact" %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
	  <%= render_breadcrumbs %>
	<% flash.each do |name, msg| %>
	  <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
	    <a class="close" data-dismiss="alert">×</a>
	    <%= msg %>
	  </div>
	<% end %>
      <div class="content">
        <div class="row">
          <div class="span9">
			<%= content_tag(:h1, (yield(:title)), :class=>"title") %>
			<%= content_tag(:h3, (yield(:subtitle)), :class=>"subtitle") %>
			
            <%= yield %>
          </div>
          <div class="span1">&nbsp;</div>

            <div class="span3">
              <div class="well sidebar-nav">
				<%= content_for?(:sidebar) ? yield(:sidebar) : render("shared/sidebar") %>
              </div>
            </div>
          </div>
        </div><!--/row-->
      </div><!--/content-->
<br style="clear:both">
      <footer>
        <p>&copy; GeekSpeak 2012 <span class="pull-right"><i>GeekSpeak</i> is a registered service mark of <a href="http://www.thedavidlawrenceshow.com/">The David Lawrence Show</a> and is used with gracious permission.</span></p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
	
    <%= user_signed_in? ? javascript_include_tag("admin") : "" %>
    <%= user_signed_in? ? stylesheet_link_tag("admin", :media => "all") : "" %>

  </body>
</html>
